<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "AI-bloom" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <%# --- GA4: gtag.js （環境変数があるときだけ読み込む） --- %>
    <% if ENV["GA_MEASUREMENT_ID"].present? %>
      <meta name="ga-measurement-id" content="<%= ENV["GA_MEASUREMENT_ID"] %>">
      <!-- Google tag (gtag.js) -->
      <script async src="https://www.googletagmanager.com/gtag/js?id=<%= ENV["GA_MEASUREMENT_ID"] %>"></script>
      <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', '<%= ENV["GA_MEASUREMENT_ID"] %>');
      </script>
    <% end %>
  </head>

  <body class="min-h-screen bg-sky-50 text-slate-800 pb-20 pt-16 flex flex-col">
    <!-- ヘッダー：ロゴ + ログイン / ログアウト -->
    <header
      style="position: fixed; top: 0; left: 0; right: 0; z-index: 40;"
      class="bg-white/90 backdrop-blur shadow-sm"
    >
      <div class="max-w-5xl mx-auto px-4 py-3 flex items-center justify-between">
        <div class="flex items-center gap-2">
          <span class="text-xl">🌻</span>
          <span class="font-bold text-lg">AI-bloom</span>
        </div>

        <div class="flex items-center gap-3 text-xs text-slate-600">
          <% if user_signed_in? %>
            <span><%= current_user.email %></span>
            <%= link_to "ログアウト",
                        destroy_user_session_path,
                        data: { turbo_method: :delete },
                        class: "hover:underline" %>
          <% else %>
            <%= link_to "ログイン", new_user_session_path, class: "hover:underline" %>
            <%= link_to "新規登録",
                        new_user_registration_path,
                        class: "px-3 py-1 rounded-full bg-sky-500 text-white text-xs" %>
          <% end %>
        </div>
      </div>
    </header>

    <!-- フラッシュメッセージ -->
    <div class="max-w-5xl mx-auto px-4 mt-3 space-y-2">
      <% flash.each do |type, message| %>
        <% color =
          if type.to_s == "notice"
            "bg-emerald-100 border-emerald-300 text-emerald-800"
          else
            "bg-rose-100 border-rose-300 text-rose-800"
          end
        %>
        <div class="border rounded-xl px-4 py-3 text-sm <%= color %>">
          <%= message %>
        </div>
      <% end %>
    </div>

    <!-- メインコンテンツ（ボトムナビぶん余白を確保） -->
    <main class="flex-1 pb-20">
      <div class="max-w-5xl mx-auto px-4 py-8">
        <%= yield %>
      </div>
    </main>

        <!-- ボトムナビ：画面下部に固定 -->
    <nav 
      style="position: fixed; left: 0; right: 0; bottom: 0; z-index: 50;"
      class="bg-white border-t border-slate-200"
    >
      <div class="max-w-5xl mx-auto flex justify-between px-4 py-2 text-xs text-slate-600">
        <%= link_to root_path,
                    class: "flex-1 flex flex-col items-center gap-1 py-1 hover:text-sky-600" do %>
          <span>🏠</span>
          <span>ホーム</span>
        <% end %>

        <%# TODO: ルート実装後に path を差し替え %>
        <%= link_to "#",
                    class: "flex-1 flex flex-col items-center gap-1 py-1 hover:text-sky-600" do %>
          <span>🧭</span>
          <span>診断</span>
        <% end %>

        <%= link_to "#",
                    class: "flex-1 flex flex-col items-center gap-1 py-1 hover:text-sky-600" do %>
          <span>🤝</span>
          <span>バディ</span>
        <% end %>

        <%= link_to "#",
                    class: "flex-1 flex flex-col items-center gap-1 py-1 hover:text-sky-600" do %>
          <span>📝</span>
          <span>投稿一覧</span>
        <% end %>

        <%= link_to "#",
                    class: "flex-1 flex flex-col items-center gap-1 py-1 hover:text-sky-600" do %>
          <span>❓</span>
          <span>その他</span>
        <% end %>
      </div>
    </nav>
  </body>
</html>

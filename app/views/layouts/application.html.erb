<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "AI-bloom" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <!-- 丸みのある日本語フォント -->
    <link href="https://fonts.googleapis.com/css2?family=Zen+Maru+Gothic:wght@400;500;700&display=swap" rel="stylesheet">

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <style>
      /* iOS/Android タップ時の黒い枠・青ハイライトを完全に消す */
      * {
        -webkit-tap-highlight-color: transparent !important;
        outline: none !important;
      }
      a, div, img, button {
        outline: none !important;
      }
    </style>
  </head>

  <body class="min-h-screen flex flex-col bg-[#FFF6E5]">
    <!-- ヘッダー -->
    <header class="sticky top-0 z-40 bg-white/90 backdrop-blur shadow-sm">
      <div class="max-w-5xl mx-auto px-4 py-3 flex items-center justify-between">
        <!-- ロゴ -->
        <div class="flex items-center gap-2">
          <%= link_to root_path, class: "inline-flex items-center" do %>
            <%= image_tag "ai_bloom_header.png",
                  alt: "AI-bloom",
                  class: "h-10 w-auto" %>
          <% end %>
        </div>

        <div class="flex items-center gap-3 text-xs text-slate-600">
          <% if user_signed_in? %>
            <span><%= current_user.email %></span>
            <%= button_to "ログアウト",
                          destroy_user_session_path,
                          method: :delete,
                          class: "hover:underline",
                          form: { class: "inline" } %>
          <% else %>
            <%= link_to "ログイン", new_user_session_path, class: "hover:underline" %>
            <%= link_to "新規登録",
                        new_user_registration_path,
                        class: "px-3 py-1 rounded-full bg-sky-500 text-white text-xs" %>
          <% end %>
        </div>
      </div>
    </header>

    <!-- フラッシュメッセージ -->
    <div class="max-w-5xl mx-auto px-4 mt-3 space-y-2">
      <% flash.each do |type, message| %>
        <% color =
          if type.to_s == "notice"
            "bg-emerald-100 border-emerald-300 text-emerald-800"
          else
            "bg-rose-100 border-rose-300 text-rose-800"
          end
        %>
        <div class="border rounded-xl px-4 py-3 text-sm <%= color %>">
          <%= message %>
        </div>
      <% end %>
    </div>

    <!-- メインコンテンツ（下ナビぶん余白を確保） -->
    <main class="flex-1 pt-2 pb-24">
      <div class="max-w-5xl mx-auto px-4 py-8">
        <%= yield %>
      </div>
    </main>

    <!-- ボトムナビ：partial を切り替え -->
    <% if (partial = current_bottom_nav_partial).present? %>
      <nav
        style="position: fixed; left: 0; right: 0; bottom: 0; z-index: 30;"
        class="bg-white/95 border-t border-slate-200"
      >
        <%= render "shared/nav/#{partial}" %>
      </nav>
    <% end %>
  </body>
</html>
